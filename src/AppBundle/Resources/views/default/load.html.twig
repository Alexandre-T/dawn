{% extends 'AppBundle::default.html.twig' %}
{% form_theme form 'AppBundle:Form:bootstrap_3_horizontal_layout_help.html.twig' %}
{% block content %}
    <h1>
        {{ 'app.nav.game' | trans }}
        <small>
            <span class="fa fa-chevron-right"></span>
            {{ 'load.title' | trans }}
        </small>
    </h1>
    <div class="row">
        <div class="col-xs-12 col-sm-6">
            <p class="text-center">
                {{ 'load.explanation' | trans }}
            </p>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
